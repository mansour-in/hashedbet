import React, { Component, PropTypes } from 'react';

export class <%= helpers.capitalize(name) %> extends Component {
  render() {
    return (
    );
  }
}

<%= helpers.capitalize(name) %>.propTypes = propTypes = {
};

export default <%= helpers.capitalize(name) %>;
